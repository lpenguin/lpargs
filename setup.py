from setuptools import setup

setup(
    name='largs',
    version='0.0.1',
    packages=['lpargs'],
    url='',
    license='',
    author='nikita',
    author_email='',
    description='',
    entry_points={
        'console_scripts': [
            'largs = lpargs.cli:main'
        ]
    }
)
